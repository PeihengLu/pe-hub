# PE Database Service Requirements
# FastAPI and web server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Data processing
pandas>=1.3.0
numpy>=1.21.0
pyarrow>=14.0.0
openpyxl>=3.0.0  # For reading Excel files

# Shared package (will be installed from local path)
# pe-common

# Catalog database (SQLite by default; set DATABASE_URL for PostgreSQL, etc.)
sqlalchemy>=2.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
