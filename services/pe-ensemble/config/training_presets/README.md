# Shipped model defaults for training / HPO fallback.
# Dataset-specific Optuna results go in ../training_presets_local/ (gitignored).
# Only promote curated shared baselines into the `datasets:` section here.
