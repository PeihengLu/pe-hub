# Pip version pins for reproducible PE-Hub installs (CLI + webserver).
# Applied via PIP_CONSTRAINT in scripts/install-clis.sh and setup-python-env.sh.
#
# Target interpreter: Python 3.11 (see environment.yml).

# Unified PyTorch across vendor models (state_dict loading).
torch>=2.0,<2.9
lightning>=2.0,<2.9

# OptiPrime / rs3: Rule Set 3 model was trained with LightGBM 3.x.
lightgbm>=3.0.0,<=3.3.5

# Core scientific stack (compatible with Python 3.11 wheels).
numpy>=1.24,<3
pandas>=1.3,<2.4
scikit-learn>=1.3,<1.7
scipy>=1.10,<1.16
